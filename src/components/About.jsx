//for all the abouts, mission, vision and who we are
const About = ({title,about}) => {
  return (
    <article>
        <h1>{title}</h1>
        <p>{about}</p>
    </article>
  )
}

export default About